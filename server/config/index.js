const localstorage = require('./localstorage.config')
const constants = require('./constants.config')
const sequelizeConfig = require('./sequelize.config')

module.exports = {
    localstorage,
    constants,
    sequelizeConfig
}